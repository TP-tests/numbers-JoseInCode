
# Fixed-Point Representation

Represent the following decimal values in fixed‑point binary:

- 12.75 using **4 integer bits** and **4 fractional bits**
- 5.125 using **3 integer bits** and **5 fractional bits**
- 7.5 using **4 integer bits** and **4 fractional bits**
